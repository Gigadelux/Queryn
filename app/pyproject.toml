[project]
name = "queryn-serve"
version = "0.1.0"
description = "Queryn migration engine — FastAPI dashboard + serial job runner (demo boilerplate)"
requires-python = ">=3.12,<3.14"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "pyyaml>=6.0",
    "numpy>=1.26",
]

[tool.uv]
package = false
